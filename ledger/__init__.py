"""The Deposit Ledger — CLI over the single SQLite file.

Schema and migrations, network enrichment, bulk import and export live here
and nowhere else (BUILD.md §3, ownership rule).
"""

__version__ = "0.1.0.dev0"

SCHEMA_VERSION = 4
